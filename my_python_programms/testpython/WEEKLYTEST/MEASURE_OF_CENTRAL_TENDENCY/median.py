

def median(hash):
  len(hash)
    hash.sort()
    if len(hash) % 2 == 0:
        n = int(n / 2)
        result = (int(hash[n]) + int(hash[n + 1])) / 2 
        print(result)
    elif len(hash) % 2 != 0:
        print(hash[n])
def mode(hash):

    
